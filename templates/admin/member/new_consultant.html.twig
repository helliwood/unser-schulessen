{% extends 'base.html.twig' %}

{% block body %}
    <div class="example-wrapper">
        <h1>Ernährungsberater zuweisen</h1>
        <div class="panel">
            <div class="row">
                <div class="d-none d-xl-block col-xl-1"></div>
                <div class="col-xl-10">
                    <h3>{{ 'Stammdaten'|trans }}</h3>
                    {{ form_start(form, {'attr': {'novalidate': 'novalidate'}}) }}
                    {{ form_widget(form) }}
                    <div class="form-group row">
                        <div class="col-sm-3"></div>
                        <div class="col-sm-9 d-flex">
                            <button type="submit" id="save" name="save" class="btn-primary btn">Speichern</button>
                            <button type="submit" id="cancel" name="cancel" class="btn-warning btn ml-auto">Abbrechen</button>
                        </div>
                    </div>
                    {{ form_end(form) }}
                </div>
                <div class="d-none d-xl-block col-xl-1"></div>
            </div>
        </div>
    </div>
{% endblock %}
