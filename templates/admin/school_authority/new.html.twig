{% extends 'base.html.twig' %}

{% block body %}
    <h1 class="text-success mb-3">Neuen Schulträger anlegen</h1>
    <div class="panel p-4 w-100">
        <div class="row">
            <div class="col-md-8">
                {{ form_start(form) }}
                {{ form_widget(form) }}
                {{ form_end(form) }}
            </div>
        </div>
    </div>
{% endblock %} 